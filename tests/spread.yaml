project: affine-mcp-server

path: /home/ubuntu/project

backends:
  lxd:
    systems:
      - ubuntu-24.04:
          username: ubuntu
          vm: true

suites:
  main/:
    summary: Test the affine-mcp-server SDK inside workshops
    environment:
      BASE/noble: ubuntu@24.04
